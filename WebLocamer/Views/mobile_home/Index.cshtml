@model IEnumerable<WebLocamer.Models.mobile_home>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.code_emplacement)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.capacité)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.terrasse_couverte)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tarif.libelle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.code_emplacement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.capacité)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.terrasse_couverte)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tarif.libelle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_mobile_home }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_mobile_home }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_mobile_home })
        </td>
    </tr>
}

</table>
