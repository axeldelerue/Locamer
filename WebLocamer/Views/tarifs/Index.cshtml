@model IEnumerable<WebLocamer.Models.tarif>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.libelle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prix_jour)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.libelle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prix_jour)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_tarif }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_tarif }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_tarif })
        </td>
    </tr>
}

</table>
